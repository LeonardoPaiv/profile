import PhotoFrame from "../PhotoFrame";
import XpBarList from "../XpBarList";

const ProfileCol = () => {
  return (
    <div>
      <PhotoFrame />
      <XpBarList />
    </div>
  );
};

export default ProfileCol;
